const Model = require("../../core/model");

class Reservation extends Model {
  constructor() {
    super("reservacion");
  }

  create(data) {}

  update(id, data) {}

  delete(id) {}
}

module.exports = Reservation;
