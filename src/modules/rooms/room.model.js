const Model = require("../../core/model");

class Room extends Model {
  constructor() {
    super("habitacion");
  }

  create(data) {}

  update(id, data) {}

  delete(id) {}
}

module.exports = Room;
